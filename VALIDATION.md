# v0.3 验证记录与验收边界

> 本文是 v0.3（当前可运行版本）的真实验证记录。v0.4 规划见 [docs/PLAN-v0.4.md](docs/PLAN-v0.4.md)。浙江发改动态列表（387 条全量翻页）与江苏发改通知公告均已接通并通过真实源验证（2026-09-09）；更多省份接入按探测结果逐站推进。

本次开发验证：2026 年 9 月 8—9 日。基于仓库初版 `d9c7956`，分支 `improve/policy-ingestion`。

## 离线回归

执行 `python -m pytest -q`：**22 项通过**（原 18 项 + 浙江 3 项 + 江苏 1 项）。包括原有 4 项及新增业务回归：

- 合成演示流程、国家发改委 HTML 适配、Web 主要页面及复核；
- 文号年份和标题括号不丢失、发布日期不误取被引用日期；
- DOCX 附件下载解析、附件变化产生版本、附件失败不覆盖已有版本；
- 相同转载关联来源、不同内容不冒充修订、保留人工决定和审核历史；
- 空列表标失败、停用来源禁止定期运行、已处理网址再次检查；
- 缺模型配置显式回退、全文尾部与附件参与分段、四标签保留、非法 JSON 字段与伪造引用进入复核；
- 定时器只跑启用的真实来源、旧库迁移、事务回滚；
- TRS 页码、域名及详情路径过滤、中国政府网 JSON 条数上限；
- 重庆下载按钮静态参数提取，不执行页面脚本；
- 模拟 HTTP 接口的请求格式、实际 usage 字段读取、截断响应失败；
- Web 写操作需要表单令牌，非法分类不得更新数据库。
- 浙江 unitbuild 构建参数提取与新旧 `/art/` 路径详情链接（标题取 `<a title>` 完整属性）；
- 浙江 art 详情元数据表解析（文件编号/生成日期/发布机构）与 JPaaS 下载网关附件；
- **浙江 unitbuild 全量翻页回归**：paramJson `pageNo` 逐页递增、第 3 页空响应即自停、跨页去重、接口 JSON 原件逐页留证（覆盖此前「mock 下仅请求 1 页且 0 链接」的问题场景）；
- 江苏 jpage 列表（script 内嵌 recordset/CDATA 记录）发现 + TRS_Editor 详情解析（标题去站点前缀）——`ListPageParser` 展开 CDATA/script 内嵌列表为通用能力。

这些测试验证程序行为，不证明真实模型语义准确率，也不代表所有官网都已接通。

## 真实官网采集

运行统计留存于 [docs/validation_summary.json](docs/validation_summary.json)。下表按每次验证的批次统计，使用规则模式；候选入库均待复核。

| 来源 | 本轮新发现 | 处理正文 | 入库候选 | 附件下载成功 | 附件解析成功 | 运行结论 |
|---|---:|---:|---:|---:|---:|---|
| 国家发改委规范性文件 | 25 | 2 | 2 | 3 | 1 | partial：一个 PDF 超过20MB限制；两个 OFD 尚不支持解析 |
| 中国政府网最新政策 | 40 | 2 | 2 | 0 | 0 | ok：公开 JSON 列表、正文与入库连通 |
| 重庆规范性文件（修正筛选后） | 6 | 2 | 2 | 4 | 4 | ok：两份投资政策，PDF与DOCX均解析成功 |

“入库候选”不等于“经确认属于投资政策”。国家及政府网样本中包含需要剔除或判断边界的文件，保留它们是为了如实呈现规则基线的限制。

重庆最初验收发现一个栏目导航被误收，随后加入详情 URL 正则筛选，并在独立新库重新运行；上表重庆数据采用修正后的批次。旧库误收记录通过人工剔除留痕，未伪造或删除失败历史。原始统计文件保留了早期和修正后的运行记录。

重庆实际政策样本：

1. [关于印发重庆市鼓励外商投资企业境内再投资若干措施的通知](https://fzggw.cq.gov.cn/zwgk/zfxxgkml/zcwj/xzgfxwj/sfzggwxzgfxwj/202609/t20260904_16034139.html)，发布日期 2026-09-04，文号 渝发改规范〔2026〕9号；下载 PDF、DOCX 各一份。
2. 《关于提升外商投资项目全生命周期服务水平的通知》，发布日期 2026-08-07，文号 渝发改规范〔2026〕8号；来源 URL、原件指纹与解析文字均保存在验证数据库。

对重庆第一份政策又执行一次单篇采集：正文和两个附件再次下载成功，`duplicates=1`、`ingested=0`、`updated=0`，库内仍为两份政策，验证了真实材料重复运行不重复入库。

## 浙江发改动态列表接通验证（2026-09-09）

栏目 `https://fzggw.zj.gov.cn/col/col1229565788/index.html` 的列表不在静态 HTML 中，而是通过 `<script … AuthorizedRead/unitbuild.js>` 声明构建参数、由前端 GET 公共页面构建接口动态生成。适配方式为「栏目页读参数 → 逐页请求构建接口（`paramJson` 携带 `pageNo`/`pageSize`/`search`）→ 解析返回的 `data.html` 列表」，不执行网页脚本。

实测（`scripts/zj_live_check.py`，2026-09-09）：

- 请求 27 页后停止（第 27 页为空响应即自停，未到 `max_pages=40` 上限），耗时约 14s；
- 去重后详情链接 **387 条**，覆盖 2026-06 新近文件至 2020-01-07 发布（最早成文 2019-12-31）；
- 新旧路径并存：新 `/col/…/art/` 结构 6 条、旧 `/art/` 结构 381 条，均被 `include` 与 `detail_url_pattern` 覆盖；
- 对最旧一条真实详情页做端到端解析：注册适配器 `_zjfgw_detail` 命中，文号 `浙发改能源〔2019〕532号`、成文 2019-12-31、发布 2020-01-07、附件 1 个（doc）均正确抽取；
- 构建接口 JSON 原件逐页落盘留证（`data/_live_check/zjfgw_gsgg/`）。

**真实入库**（隔离库，`--source zjfgw_gsgg --prefer rule --limit 15`，2026-09-09）：发现 387 条 → 下载/解析 15 条全部入库待复核；附件 11 个下载成功、2 个网关限流失败（可补采自愈）、6 个 WPS/旧 DOC 保留原件待解析；分类样例命中准确（招投标监管→准入/激励约束、新能源上网电价改革→激励约束/准入等）。

本次为**列表发现链路验证**，未对 387 条全量执行正文入库；入库由 `run_source` 对发现链接逐条 `ingest`。

## 江苏发改通知公告接通验证（2026-09-09）

来源 `jsfgw_tzgg`（江苏省发展改革委 · 通知公告 col284）。站点机制为 hanweb/TRS jpage：列表首页由服务端以 `<script>` 内嵌 `<datastore><recordset><record><![CDATA[<li><a…>]]>` XML 序列化输出；`ListPageParser` 已增加展开该内嵌记录的通用能力（剥 CDATA 后并入解析），详情页正文 `div.TRS_Editor`，新增 `site_adapters._jsfgw_detail` 清洗 `<title>` 站点前缀。

实测（隔离库，`--source jsfgw_tzgg --prefer rule --limit 5`，2026-09-09）：

- 列表发现 30 条（最新公告），`detail_url_pattern` 精确锁定 `art_284_` 栏目码，解读/导航链接零误收；
- 两批真实入库 7 条待复核，重复运行 policy_key 无重复（幂等成立）；规则排除 3 条非政策内容；
- 该栏目混有成品油调价、招聘公示等非政策文件，待复核噪音较高；投资政策价值有限，业务使用建议后续改接规范性文件目录。

## 来源覆盖

| 层级 | 当前情况 | 继续工作 |
|---|---|---|
| 中国政府网 | 栏目 HTML 中公开的 ZUIXINZHENGCE.json 已适配并实际入库 | 根据运行稳定性扩大页数与批次，校验发现召回率 |
| 国家发改委 | 保留两栏目配置；规范性文件已有实际原件下载和入库 | 处理较大附件、OFD，检查通知栏目实时变化 |
| 浙江省发改委 | 动态列表已接通并真实验证：unitbuild → 构建接口逐页翻页，387 条详情链接（2020-01 至 2026-06）；15 条真实入库 | 配置真实模型后全量入库并连续运行两轮；旧路径正文偏短页（印发通知+附件承载正文）需抽样确认抽取质量 |
| 江苏省发改委 | 通知公告栏目已接通：recordset(CDATA) 列表发现 30 条 + 真实入库 7 条 | 业务价值更高的规范性文件目录为 JS 渲染待适配；历史翻页 dataproxy 待接入 |
| 外省 | 重庆规范性文件作为外省试点完成采集验证 | 尚未覆盖全部省级发改委，按业务优先级逐站补配置、适配和验收 |

部分其他站点尝试返回 403、412 或超时；这些不作为成功接入证据。不能以“配置了网址”代替“已采集成功”。

## 尚不能宣称完成的事项

1. 未配置 `LLM_API_KEY`、`LLM_MODEL`，本次真实模型调用数为零，无真实分类准确率或推理成本结论。模拟接口测试不替代真实 API 验证。
2. 浙江列表发现链路已接通（见上），但**未对 387 条执行全量正文入库与双轮连续性运行**；旧路径页面正文有时仅含印发语、正文实体在 doc 附件中，需抽样确认入库正文质量后，再宣称浙江栏目全量接入完成。
3. 重庆只是外省试点，不代表全国各省覆盖。
4. 未部署长期后台服务；用户启动 `schedule` 后才会定期运行。
5. PDF 文本解析不包含 OCR；OFD、旧 DOC、表格附件和压缩包尚未实现正文解析，均保留原件及处理状态。
6. 没有完整有效性知识库，不自动断言政策当前有效；成文日、发布日期、同文号及采集版本都不等同于法律效力判断。

## 建议的最终验收

先配置单位允许的模型服务，用同一批独立人工标注样本评价相关性、四类分类及复核耗时；再验收浙江与优先外省的列表发现、标题日期文号、正文及附件。至少连续运行两轮，检查新文件新增、相同文件不重复、失败补采及人工确认不被覆盖。业务准确率的目标值应由指导员结合使用方式确定，不能把未经测量的百分比写成已经达到。
