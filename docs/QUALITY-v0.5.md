# v0.5 质量审查与修复

基线：`bbef2ab327bc26a7cb8791dc4cd60d8dcb74702c`；日期：2026-09-09。

## 结论

最新新增工作主要在 56 条浙江标注、LLM 对比脚本和全量双轮检查脚本。省份采集能力已形成基础，本版优先修复会令结果失真的流程问题，不继续堆来源数量或根据未终审标签直接收紧生产规则。

| 原问题 | 影响 | 修复 |
|---|---|---|
| 对比脚本只构造网页正文，漏传已解析附件 | 实际用于分类的材料不足，和生产流程不一致 | 读取 attachments，当前规则与 LLM 使用同一 Document，并记录输入指纹 |
| 复制 SQLite 主文件未处理 WAL；先删除目标文件 | 可能漏掉刚写入的数据，路径重叠时可能破坏原库 | 原库只读打开，SQLite backup 到新文件；已有副本和原库均拒绝覆盖 |
| 历史库预测直接被称为规则基线 | 可能是旧版规则、人工结果或其他模型，不是同条件对比 | 同时列历史结果、当前规则重算、当前模型路径三组 |
| 回退规则也累加 llm_classified，异常保留旧预测 | 看似模型成功，实际是回退或根本未执行 | 分开统计模型响应、回退、异常、未执行；连续三次服务失败停止后续调用 |
| 指标在两个脚本中重复实现，比较脚本缺少标签校验 | 重复标签、错误布尔值等可污染成绩 | 共用评价模块，校验类型、类别、重复项、样本版本，明确 pending 与覆盖率 |
| 样本仅依赖数据库 ID | 重建库后可能评到另一篇文件 | 从已有快照补入 URL + content_sha256；按来源版本定位，再核对标题/文号 |
| README/工作簿称人工金标准，末尾却说明为 AI 初标 | 容易把与 AI 标签的一致率说成真实准确率 | 保留原标签，统一标记 ai_draft；探索比较需 --allow-provisional |
| full check 自动 rmtree，半数以下失败也可通过 | 既有数据可能丢失，不完整采集可能被宣称完成 | 不删除目录；新建隔离目录或显式续跑，按覆盖、材料、运行、双轮分别判定 |
| 达到配置页数上限与真正翻到末页未区分 | 无法证明全量发现 | 浙江发现器记录 pages_fetched / end_reached，全量检查据此判定 |
| 引用标题就能通过证据校验；非相关却返回类别可放行 | 条款判断缺少可定位依据、结果矛盾 | 引用须在当前正文/附件段落；矛盾输出转待复核；只汇总有效相关段落的类别 |
| 独立评测绕过 Pipeline 材料完整性检查 | 缺附件仍可能被直接排除 | 材料检查移入分类器统一执行，未完整读取的 no 转 pending |
| API 非正常完成/重复 JSON 字段未严判，后续分段失败丢用量 | 不可靠输出可能被使用，成本统计偏低 | 拒绝截断、过滤、拒答及重复字段；累加服务返回的已知 token 用量 |

## 验证

- `python -m pytest -q`：47 项通过；本轮新增 13 项针对性回归。
- 样本结构检查：56 条，全部绑定来源 URL 和原采集内容指纹，全部 `ai_draft`；保留原来的 37 条相关、19 条非相关及四类分布。
- SQLite WAL、原库保护、全文附件同输入、服务回退、离线预检、未处理候选、附件不完整、页数未到底、重复字段及分段失败用量都有回归覆盖。
- 既有分段测试的模拟证据原来引用标题，已改为引用当前输入段落，符合新要求。
- 本环境没有用户本机的 API 凭证及 `/tmp/pc_zj_full/policy.db` 原采样库，未开展 56 条真实模型评测，也未重跑 387 条全量采集。没有给出新的准确率、召回率或全量验收通过结论。

> **补充（2026-09-10，用户本机环境）**：上述两项已补做。① 用本版口径重跑 56 条真实模型评测（`deepseek-v4-flash`，
> `--allow-provisional`，指纹绑定 56/56，0 失败/0 回退/0 截断，耗时 10m49s）：relevance 精确率 0.619→**0.906**、
> F1 0.658→**0.841**、整篇一致率 0.089→**0.375**，`pending` 17 条（严格计错）；结论与逐条分析见
> [`gold/zj_v1_20260909/EVAL-REPORT.md`](../gold/zj_v1_20260909/EVAL-REPORT.md)。
> ② 重跑浙江全量双轮：387 候选 / 348 下载 / 323 入库，单轮约 286s（规则模式），第二轮 0 新增、263 去重、7 补采，
> 稳定输入幂等；仍属候选库验证，**不等于全量验收通过**。

## 仍需业务确认

初标把部分招投标监管、信用制度、政策清理文件统一归为非投资政策，但任务要求也包含项目全过程监管、责任约束；两者边界不能仅凭标题或通用关键词认定。价格政策也应区分民生收费与直接影响项目收益的支持机制。本版保留原标签并标明其待核定状态，不将这些排除口径直接固化到生产分类器。

当前样本来自已入库政策，且原文快照仅节选前 600 字，缺少被采集阶段排除的样本；即便后续与全部标签一致，也不能据此证明全流程召回率。后续先核对完整正文及附件，再完成业务终审，并另留不参与提示词调优的人工验收集。

自动判断中仍有长文分段可能切断上下文、部分附件格式未解析、官网稳定性等限制。人工复核标志、原文与附件状态应与分类结果一起使用。
