# 怎么把这个系统跑起来

三步，全程在自己电脑上操作，不需要服务器。

---

## 第 1 步：装依赖（只做一次）

打开「终端」，进到项目文件夹：

```bash
cd 你放这个项目的目录
```

然后建一个独立的 Python 环境并装依赖：

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
```

> **需要 Python 3.9 或更高版本**（实测 3.9.6 和 3.13 都能跑）。
> 在终端敲 `python3 --version` 能看到版本号。太低的话用 `brew install python` 装一个。

> 仓库里如果已经有一个 `.venv` 目录，可以直接用，但**建议自己重建**——它可能是别人在别的机器上建的。

---

## 第 2 步：配大模型密钥（可选，只做一次）

**不配也能用**——系统会用本地规则判断政策。配了之后判断更准（能处理语义歧义）。

```bash
.venv/bin/python -m policy_collector.cli configure-llm
```

按提示填服务地址和密钥。**密钥不会在屏幕上回显，也不会进 git**。

填完可以检查一下通不通：

```bash
.venv/bin/python -m policy_collector.cli llm-check
```

---

## 第 3 步：启动

```bash
.venv/bin/python -m policy_collector.cli web --port 8000
```

终端出现这一行就成功了：

```
* Running on http://127.0.0.1:8000
```

**浏览器打开 <http://127.0.0.1:8000>** 就能用了。

想让它自动帮你打开浏览器，加 `--open`：

```bash
.venv/bin/python -m policy_collector.cli web --port 8000 --open
```

**停止服务**：回到终端按 `Ctrl + C`。

> 8000 端口被占用时，换一个号，比如 `--port 8080`。
> 服务只监听本机（`127.0.0.1`），外网访问不到。

---

## 页面上都有什么

| 页面 | 用来干什么 |
|---|---|
| **工作总览** | 库存多少、覆盖哪些省、最近采了什么 |
| **政策资料库** | 搜政策、看正文和附件 |
| **待办清单** | **日常干活从这里开始**：哪些等你拍板、哪些机器自己修 |
| **省份浏览** | 按省份看覆盖情况 |
| **采集来源** | 启动采集，包括"一键全国采集" |
| **处理进度** | 采集跑到哪一步、有没有失败 |
| **材料质量** | 附件解析情况、哪些需要处理 |
| **模型配置** | 填／改大模型密钥 |
| **数据维护** | 清空数据库（**会先自动备份**） |

---

## 常见问题

**想从零开始，不要现在这些数据？**

去「数据维护」页点「清空政策库」。它会**先自动备份**到 `data/backups/`，
清空了也能捞回来（恢复方法页面上有写）。来源配置不会被清掉。

**想用另一套数据（比如演示时不想动正式库）？**

启动时加一个数据目录：

```bash
POLICY_DATA_DIR=/tmp/demo .venv/bin/python -m policy_collector.cli web --port 8000
```

**采集某个省时提示需要 playwright？**

少数省份（如湖北）的官网有防爬验证，需要用真实浏览器过一次验证。装一次就行：

```bash
.venv/bin/pip install playwright
```

不装也不影响其他省份的采集。

**不想开界面，直接用命令行采集？**

先查有哪些来源：

```bash
.venv/bin/python -m policy_collector.cli sources
```

再跑：

```bash
.venv/bin/python -m policy_collector.cli run --source 来源名 --limit 10
```

**数据存在哪？**

- 数据库：`data/policy.db`
- 下载的网页原文和附件：`data/downloads/`
- 备份：`data/backups/`

**`README.md` 那么长，是干什么的？**

那是给评审和汇报用的（讲清成果、业务口径、验收记录），**不是上手文档**。
上手看这一份就够了。
